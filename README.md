# kombine-bilby

Plugin for using kombine with bilby.

This plugin exposes the `kombine` sampler via the `bilby.samplers` entry point.
Once installed, you can select it in `bilby.run_sampler` using `sampler='kombine'`.

## Installation

The package can be install using pip

```
pip install kombine-bilby
```
